{
  "name": "2aio-dashboard",
  "version": "0.2.0",
  "description": "調査 → 計画 → 実装 → テスト → Push を、フェーズごとの権限つきで通すダッシュボード",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "@openai/codex-sdk": "^0.146.0"
  }
}
